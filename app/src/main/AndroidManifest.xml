<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/list"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/list"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImageUpload"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="PCC -ADA/Skybox Operator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
<!-- Android reads the manifest before the app runs -->
<!-- The manifest tells the Android system about your app — what it contains, what it can do, and what permissions it needs -->